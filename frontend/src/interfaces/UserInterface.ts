export interface IUser {
    firstName: string,
    lastName: string,
    username: string,
    bio: string,
    privacyMode: number,
    friends: string[],
    friendRequests: string[],
    sunsets: string[],
}